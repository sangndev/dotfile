# For vibe coding

- If you're a vibe coder, make sure to install `goose`, which is an AI agent developed by Block (the company behind Square, Cash App...). Because the neovim's plugin for vibe code we use is using `goose` under the hood.
- Install [goose](https://block.github.io/goose/docs/getting-started/installation/)
